@model IEnumerable<scfs_erp.Models.ExportShippingBillMaster>

@{
    ViewBag.Title = "Export Shipping Bill";
}

<div class="col-lg-12">
    <div class="panel panel-primary" style="background: #323C55;">
        <div class="panel-heading">
            <div class="panel-title">
                <i class="glyphicon glyphicon-export"></i>@ViewBag.Title


                @*<a class="btn btn-success btn-sm pull-right" style="padding:3px;margin:0 0 10px" href="@Url.Action("Form", "ExportShippingBillMaster")"><i class="fa fa-plus-square "></i> Add New </a>*@
            </div>
        </div>
    </div>
    <div class="row">
        <div class="col-lg-12">
            <div class="panel-body">
                <div class="row">
                    <div class="col-xs-3"> </div>
                    <div class="col-xs-6">
                        <form action="#" method="post" accept-charset="utf-8" id="exportloadslipform" data-ng-controller="myController" name="exportloadslipform">
                            <label for="from">From</label>
                            <input type="text" id="from" name="from" value="@System.Web.HttpContext.Current.Session["SDATE"]">
                            <label for="to">to</label>
                            <input type="text" id="to" name="to" value="@System.Web.HttpContext.Current.Session["EDATE"]">
                            <input type="submit" name="submit" value="Search" class="btn btn-primary btn-xs" />
                        </form>
                    </div>
                </div>
                <br />
                <table class="table table-bordered table-striped table-hover table-heading table-datatable" id="dataTables-example">
                    <thead>
                        <tr>

                            <th>
                                Date
                            </th>
                            <th>
                                No
                            </th>
                            <th>
                                Exporter Name
                            </th>
                            <th>
                                CHA Name
                            </th>
                            <th>
                                VEHICLE No
                            </th>
                            <th>
                                Nop
                            </th>
                            <th>
                                Weight
                            </th>
                            <th></th>
                        </tr>
                    </thead>
                </table>
            </div>
            <input id="ESBDIdx" type="text" class="hide" value="@Session["ESBDIdx"]" />
        </div>
    </div>
</div>

@section Scripts {
    <script>
    $(document).ready(function () {

        $('#dataTables-example').dataTable({
            "bServerSide": true,
            "sAjaxSource": "@Url.Action("GetAjaxData", "ExportShippingBillMaster")",
            "bProcessing": true,
            "bStateSave": true,
            "aaSorting": [[1, "desc"]],
            "oLanguage": {
                "sSearch": "Search: "
            },
            "aoColumns": [
                { "sName": "", "sClass": "col-lg-2" },
                { "sName": "", "sClass": "col-lg-2" },
                { "sName": "", "sClass": "col-lg-3" },
                { "sName": "", "sClass": "col-lg-3" },
                { "sName": "", "sClass": "col-lg-2" },
                { "sName": " ", "sClass": "col-lg-2" },
                { "sName": "", "sClass": "col-lg-2" },
                {
                    "sClass": "col-sm-1 cent",
                    "sName": "ID",
                    "bSearchable": false,
                    "bSortable": false,
                    "fnRender": function (oObj) {
                        if (oObj.aData[8] == 0) {
                        //if (oObj.aData[5] == 1.00) {
                              return ' <a   class="btn btn-primary btn-xs"   href="@Url.Action("Edit", "ExportShippingBillMaster")/' +
                                oObj.aData[7] + '\"><i class="glyphicon glyphicon-edit"></i></a>  &nbsp;&nbsp;<a   class="btn btn-warning btn-xs cancel"   href="#" id="' + oObj.aData[7] + '" title="Cancel">C</a>&nbsp;&nbsp;<a   class="btn hide btn-default btn-xs"   href="@Url.Action("PrintView", "ExportShippingBillMaster")/' +
                                oObj.aData[7] + '\"  target="_blank"><i class="glyphicon glyphicon-print"></i></a>&nbsp;&nbsp; <a  class="btn btn-warning btn-xs"  href="@Url.Action("BForm", "ExportShippingBillMaster")/' +
                                oObj.aData[7] + '~' + oObj.aData[9] + '\"  " data-toggle="modal" data-target="#myModal" title="Original Shipping Bill Details" data-backdrop="static" data-keyboard="false">T</a>  &nbsp;&nbsp<a  class="btn btn-danger del btn-xs" href="#" id="' + oObj.aData[7] + '" "><i class="glyphicon glyphicon-trash"></i> </a> ';

                              //return ' <a   class="btn btn-primary btn-xs"   href="@Url.Action("Edit", "ExportShippingBillMaster")/' +
                               // oObj.aData[7] + '\"><i class="glyphicon glyphicon-edit"></i></a>  &nbsp;&nbsp;<a   class="btn btn-warning btn-xs cancel"   href="#" id="' + oObj.aData[7] + '" title="Cancel">C</a>  &nbsp;&nbsp;<a   class="btn hide btn-default btn-xs"   href="@Url.Action("PrintView", "ExportShippingBillMaster")/' +
                               // oObj.aData[7] + '\"  target="_blank"><i class="glyphicon glyphicon-print"></i></a>  &nbsp;&nbsp;<a  class="btn btn-danger del btn-xs" href="#" id="' + oObj.aData[7] + '" "><i class="glyphicon glyphicon-trash"></i> </a> ';

                        }
                        else {
                             // return ' <a   class="btn btn-primary btn-xs"   href="@Url.Action("Edit", "ExportShippingBillMaster")/' +
                               // oObj.aData[7] + '\"><i class="glyphicon glyphicon-edit"></i></a>  &nbsp;&nbsp;<a   class="btn btn-warning btn-xs cancel"   href="#" id="' + oObj.aData[7] + '" title="Cancel">C</a>&nbsp;&nbsp;<a   class="btn hide btn-default btn-xs"   href="@Url.Action("PrintView", "ExportShippingBillMaster")/' +
                               // oObj.aData[7] + '\"  target="_blank"><i class="glyphicon glyphicon-print"></i></a>&nbsp;&nbsp; <a  class="btn btn-warning btn-xs"  href="@Url.Action("BForm", "ExportShippingBillMaster")/' +
                               // oObj.aData[7] + '~' + oObj.aData[9] + '\"  " data-toggle="modal" data-target="#myModal" title="Original Shipping Bill Details" data-backdrop="static" data-keyboard="false">T</a>  &nbsp;&nbsp<a  class="btn btn-danger del btn-xs" href="#" id="' + oObj.aData[7] + '" "><i class="glyphicon glyphicon-trash"></i> </a> ';


                            return ' <a   class="btn btn-primary btn-xs"   href="@Url.Action("Edit", "ExportShippingBillMaster")/' +
                                oObj.aData[7] + '\"><i class="glyphicon glyphicon-edit"></i></a>  &nbsp;&nbsp;<a   class="btn btn-warning btn-xs cancel"   href="#" id="' + oObj.aData[7] + '" title="Cancel">C</a>  &nbsp;&nbsp;<a   class="btn hide btn-default btn-xs"   href="@Url.Action("PrintView", "ExportShippingBillMaster")/' +
                                oObj.aData[7] + '\"  target="_blank"><i class="glyphicon glyphicon-print"></i></a>  &nbsp;&nbsp;<a  class="btn btn-danger del btn-xs" href="#" id="' + oObj.aData[7] + '" "><i class="glyphicon glyphicon-trash"></i> </a> ';
                        }
                    }
                }
            ]
        });
        $("#from").datepicker({
            defaultDate: "+1w",
            changeMonth: true,
            numberOfMonths: 3,
            dateFormat: 'dd-mm-yy',
            onClose: function (selectedDate) {
                $("#to").datepicker("option", "minDate", selectedDate);
            }
        });
        $("#to").datepicker({
            defaultDate: "+1w",
            changeMonth: true,
            numberOfMonths: 3,
            dateFormat: 'dd-mm-yy',
            onClose: function (selectedDate) {
                $("#from").datepicker("option", "maxDate", selectedDate);
            }
        });
       //---------------Delete Record-------------------//
        $(document).on("click","a.del",function() {
            var r=confirm("Are you sure to delete this record ?");

            if (r==true)
            {
                var idx = $(this).attr('id');
                base = window.location.origin;
                $.post(base + "/ExportShippingBillMaster/Del",
                         {
                             id: idx, fld: "EXPORTSHIPPINGBILLMASTER"
                         },
                       function (data, status) {
                           if (data == "") {
                               //alert("You are not Authorized to Delete....!");

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + 'You are not Authorized to Delete....!' + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                           }
                           else if (data != "Deleted Successfully ...") {
                               //alert(data);

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + data + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                           }
                           else {
                               //alert(data);

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + data + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                               location.replace(base + "/ExportShippingBillMaster");
                           }
                       });
            }
            return false;
        });//end

        //---------------Delete Record-------------------//
        $(document).on("click", "a.cancel", function () {
            var r = confirm("Are you sure to Cancel this Shipping Bill ?");

            if (r == true) {
                var idx = $(this).attr('id');
                base = window.location.origin;
                $.post(base + "/ExportShippingBillMaster/CancelBill",
                         {
                             id: idx, fld: "EXPORTSHIPPINGBILLMASTER"
                         },
                       function (data, status) {
                           if (data == "") {
                               //alert("You are not Authorized to Cancel....!");

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + 'You are not Authorized to Cancel....!' + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                           }
                           else if (data != "Cancelled Successfully ...") {
                               //alert(data);

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + data + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                           }
                           else {
                               //alert(data);

                               $('<div></div>').dialog({
                                   modal: true,
                                   title: "Information",
                                   open: function () {
                                       var markup = '<b>' + data + '<b>';
                                       $(this).html(markup);
                                   },
                                   buttons: {
                                       Ok: function () {
                                           $(this).dialog("close");
                                       }
                                   }
                               }); //end confirm dialog
                               location.replace(base + "/ExportShippingBillMaster");
                           }
                       });
            }
            return false;
        });//end
    });

    </script>
}


<div class="modal" id="myModal" tabindex="-1" role="dialog" aria-labelledby="myModalLabel" aria-hidden="true">

</div>