@model IEnumerable<scfs_erp.Models.ExportTariffGroupMaster>
@{
    ViewBag.Title = "Export Tariff Group";
}
<div class="row">
    <div class="col-xs-12">
        <div class="box">
            <div class="box-header">
                <div class="box-name">
                    <i class="glyphicon glyphicon-share"></i><strong>@ViewBag.Title</strong>
                </div>
                <div class="box-icons">
                    <button onclick="parent.location = '@Url.Action("Form","ExportTariffGroupMaster")'; return false;" class=" btn-success"> Create New </button>
                </div>
                <div class="no-move"></div>
            </div>
            <div class="box-content no-padding">
                <br />
                <table class="table table-bordered table-striped table-hover table-heading table-datatable" id="dataTables-example">
                    <thead>
                        <tr>
                            <th>
                                Code
                            </th>
                            <th>
                                Description
                            </th>
                            <th>Status</th>
                            <th class=""></th>
                        </tr>
                    </thead>
                    @*</thead>
                        <tbody>
                            @foreach (var item in Model)
                            {
                                <tr>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ACHEADGDESC)
                                    </td>
                                    <td>
                                        @Html.DisplayFor(modelItem => item.ACHEADGCODE)
                                    </td>
                                    <td>
                                        @if (item.DISPSTATUS != 0)
                                        {

                                            <span>Enabled</span>

                                        }
                                        else
                                        {
                                            <span>Disabled</span>

                                        }
                                    </td>
                                    <td class="col-sm-2 cent">
                                        @Html.ActionLink("Edit", "Form", new { id = item.ACHEADGID }, new { @class = "btn btn-primary" })
                                        <a href="#" class="btn btn-danger del" id="@item.ACHEADGID">  Delete </a>
                                    </td>
                                </tr>
                            }
                        </tbody>*@
                </table>
            </div>
        </div>
    </div>
</div>

@section Scripts {
    <script>
        $(document).ready(function () {

            $('#dataTables-example').dataTable({
                "bServerSide": true,
                "sAjaxSource": "@Url.Action("GetAjaxData", "ExportTariffGroupMaster")",
                "bProcessing": true,
                "bStateSave": true,
                "aaSorting": [[1, "desc"]],
                "oLanguage": {
                    "sSearch": "Search: "
                },
                "aoColumns": [
                    { "sName": "" },
                    { "sName": " ", "sClass": "action" },
                    {
                        "sName": "", "sClass": "action",
                    },
                    {
                        "sClass": "col-sm-2 cent",
                        "sName": "ID",
                        "bSearchable": false,
                        "bSortable": false,
                        "fnRender": function (oObj) {
                            return ' <a   class="btn btn-primary btn-xs"   href="@Url.Action("Edit", "ExportTariffGroupMaster")/' +
                                oObj.aData[3] + '\" Title=Modify Details"><i class="glyphicon glyphicon-edit"></i></a>  &nbsp;&nbsp;<a  class="btn btn-danger btn-xs del" href="#" id="' + oObj.aData[3] + '" "><i class="glyphicon glyphicon-trash"></i> </a> ';
                        }
                    }
                ]
            });

            //-----------Delete Row function--------------//
            $(document).on("click", "a.del", function () {
                var r = confirm("Are you sure to delete this record ?");

                if (r == true) {
                    var idx = $(this).attr('id');
                    base = window.location.origin;

                    $.post(base + "/ExportTariffGroupMaster/Del",
                        {
                            id: idx, fld: "ExportTariffGroupMaster"
                        },
                        function (data, status) {
                            alert(data);
                            if (data == "Deleted Successfully ...")
                                location.replace(base + "/ExportTariffGroupMaster");

                        });//---End of post
                }
                return false;
            });//End of delete
        });
    </script>
}


